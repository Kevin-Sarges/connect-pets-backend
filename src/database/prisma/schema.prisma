generator myClient {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Cidades {
  id          String @id @default(uuid())
  nome_cidade String

  @@map("cidades")
}
